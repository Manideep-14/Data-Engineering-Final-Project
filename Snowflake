USE DATABASE SUPPLY_CHAIN_DB;
USE SCHEMA PROJECT_SCHEMA;

SHOW TABLES;
USE DATABASE SUPPLY_CHAIN_DB;
USE SCHEMA PROJECT_SCHEMA;

SHOW TABLES;
USE DATABASE SUPPLY_CHAIN_DB;
USE SCHEMA PROJECT_SCHEMA;

CREATE OR REPLACE TABLE SUPPLY_CHAIN_500 (
ID NUMBER
);

SHOW TABLES;
USE DATABASE SUPPLY_CHAIN_DB;
USE SCHEMA RAW;

SELECT * FROM SUPPLYCHAIN_DATA LIMIT 10;
SELECT * FROM ACCESS_LOGS LIMIT 10;
DESC TABLE SUPPLYCHAIN_DATA;
DESC TABLE ACCESS_LOGS;

SELECT SHIPPING_MODE, COUNT(*) AS TOTAL_ORDERS
FROM SUPPLYCHAIN_DATA
GROUP BY SHIPPING_MODE
ORDER BY TOTAL_ORDERS DESC;

SELECT DELIVERY_STATUS, COUNT(*) AS TOTAL_ORDERS
FROM SUPPLYCHAIN_DATA
GROUP BY DELIVERY_STATUS
ORDER BY TOTAL_ORDERS DESC;

SELECT CATEGORY_NAME, COUNT(*) AS TOTAL_ORDERS
FROM SUPPLYCHAIN_DATA
GROUP BY CATEGORY_NAME
ORDER BY TOTAL_ORDERS DESC;

SELECT ORDER_REGION, COUNT(*) AS TOTAL_ORDERS
FROM SUPPLYCHAIN_DATA
GROUP BY ORDER_REGION
ORDER BY TOTAL_ORDERS DESC;
CREATE OR REPLACE TABLE GOLD_SUPPLYCHAIN AS
SELECT *
FROM SUPPLYCHAIN_DATA;

SELECT COUNT(*) FROM GOLD_SUPPLYCHAIN;
SHOW TABLES;